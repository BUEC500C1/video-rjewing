Put all your docker secret files here. This allows you to use docker secrets without running a docker swarm.

The file should have the same name the same as the environment variable fallback, eg:
`docker/secrets/EC500_TWITTER_API_KEY`
